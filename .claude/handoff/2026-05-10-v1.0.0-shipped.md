# Handoff — 2026-05-10

## TL;DR

v1.0.0 is shipped to GitHub (`594d92b`). Extension is installed locally
and working. Nothing in flight. Next session can pick up
`v1.1.0`-candidate keymap additions or marketplace publish.

## What's live right now

- **Repo HEAD**: `594d92b feat: v1.0.0 — language-aware refactoring +
  IntelliJ Mac keymap` on `origin/main`.
- **Locally installed extension**: `jungsehui.custom-intellij-nav@1.0.0`.
- **`.idea/`** is on disk but ignored by git (added to `.gitignore`).
- **`.planning/todos/pending/`** has 1 todo:
  `2026-04-28-identify-next-intellij-keymap-additions-for-v1-1-0.md`
  (k--kato gap analysis with HIGH/MED/LOW priorities).

## Loose ends

1. **Marketplace publish not done.** The repo is ready
   (`publisher: jungsehui`, GitHub public, `package.json` has homepage
   and bugs URLs). Needs an Azure DevOps PAT with Marketplace scope and
   a `vsce login jungsehui` + `vsce publish`.

2. **CI builds VSIX but doesn't publish.** `.github/workflows/release.yml`
   has a `publish` job stub gated on `refs/tags/v*`. It's empty past
   `actions/setup-node`. If we want auto-publish on tag, fill it in
   with `vsce publish` + `secrets.VSCE_PAT`.

3. **`.claude/` documentation is brand new** (this commit). Future
   sessions should read `CLAUDE.md` first, then drill into
   `.claude/architecture.md`, `.claude/workflows.md`,
   `.claude/conventions.md` as needed.

## What to do next (pick one)

### Option A — Ship to Marketplace (~1 hour)

1. Generate Azure DevOps PAT (Marketplace → Manage → All accessible
   organizations).
2. `npx vsce login jungsehui`, paste PAT.
3. `npx vsce publish` from a clean working tree.
4. Verify install via `code --install-extension jungsehui.custom-intellij-nav`
   from a fresh machine or Code-Insiders.
5. Add the resulting marketplace badge to README.md.
6. Tag `v1.0.0` if not already, push tag.

### Option B — Cut v1.1.0 with the keymap gaps (~2-3 hours)

Read `.planning/todos/pending/2026-04-28-...` for the full gap analysis.
TOP-10 high-impact additions:

| Mac key | Command | Category |
|---|---|---|
| `alt+left` / `alt+right` | `cursorWordLeft/Right` | editing |
| `alt+shift+left/right` | `cursorWordLeft/RightSelect` | editing |
| `alt+backspace` / `alt+delete` | `deleteWordLeft/Right` | editing |
| `cmd+-` / `cmd+=` | `editor.fold` / `editor.unfold` | editing |
| `cmd+g` / `cmd+shift+g` | `nextMatchFindAction` / `previousMatchFindAction` | search |

Then bump `package.json` to `1.1.0`, update CHANGELOG, run `/ship`,
commit, push, optionally publish.

### Option C — CI auto-publish on tag (~30 min)

Fill in the `publish` job in `.github/workflows/release.yml` with
`vsce publish -p ${{ secrets.VSCE_PAT }}`. Add the secret in GitHub
repo settings. Test by tagging `v1.0.1`.

## Where things are

```
README.md                  # User-facing docs (already v1.0.0-current)
CHANGELOG.md               # Version history (v1.0.0 entry written)
LICENSE.txt                # MIT
src/                       # Source (see .claude/architecture.md)
package.json               # Manifest (5 cmds + 80 keys + 12 cfgs)
.github/workflows/         # CI (build only, publish stub)
.planning/todos/pending/   # Backlog (1 entry: v1.1.0 keymap gaps)
.claude/                   # AI context (this folder)
```

## Quick environment refresher

- Node 22 via nvm: `export PATH="$HOME/.nvm/versions/node/v22.22.1/bin:$PATH"`
  (vsce needs global `File`, only on Node 20+).
- gh CLI has both `jungsehui` and `jungsehui202` authenticated;
  `jungsehui202` is default, repo is owned by `jungsehui`. Switch
  before pushing: `gh auth switch -u jungsehui && gh auth setup-git`.
