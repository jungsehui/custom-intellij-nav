out
dist
node_modules
.vscode-test/
*.vsix

.vscode/**
sample-workspace/**
out/test/**
.vscode-test.mjs
vsc-extension-quickstart.md
**/*.map

# IDE / workspace tooling metadata (not part of repo)
.idea/
.planning/
.omc/
.cursor/

# .claude/ is tracked — it's the AI session context for this repo.
# Only ignore user-local overrides under .claude/local/.
.claude/local/
